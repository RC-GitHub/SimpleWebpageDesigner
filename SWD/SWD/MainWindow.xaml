<Window x:Class="SWD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWD"
        mc:Ignorable="d"
        Title="MainWindow"
        MinHeight="350" 
        MinWidth="500"
        Height="450"
        Width="800"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <Grid x:Name="gdMain">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="{Binding BackgroundGradStart}"/>
                <GradientStop Color="{Binding BackgroundGradEnd}" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="0,10,0,0"/>
                    <Setter Property="Width" Value="350"/>
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="Foreground" Value="{Binding ButtonFont}" />
                    <Setter Property="Background" Value="{Binding ButtonPrimary}" />
                    <Setter Property="BorderBrush" Value="{Binding ButtonSecondary}" />
                    <Setter Property="BorderThickness" Value="4" />
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                </Style>
            </StackPanel.Resources>
            <Label x:Name="lblMainTitle" Content="SimpleWebpageDesigner" FontFamily="Century Gothic" FontSize="36" Height="60" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="SemiBold"/>
            <Button x:Name="btnNewProject" Content="New project" Click="btnNewProject_Click" />
            <Button x:Name="btnOpenExisting" Content="Open existing" HorizontalAlignment="Center"/>
            <Button x:Name="btnDeleteExisting" Content="Delete existing" Click="btnDeleteExisting_Click"/>
            <Button x:Name="btnThemeSettings" Content="Theme settings" Click="btnThemeSettings_Click"/>
            
        </StackPanel>
    </Grid>
</Window>
