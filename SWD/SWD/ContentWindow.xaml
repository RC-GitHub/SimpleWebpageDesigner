<Window x:Class="SWD.ContentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWD"
        mc:Ignorable="d"
        Title="ContentWindow" Height="1000" Width="1000">
    <Grid x:Name="gdContent">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF97C9EF"/>
                <GradientStop Color="#FF65ACE2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ToolBarTray DockPanel.Dock="Top">
                <ToolBar>
                    <Button ToolTip="Make a new file." Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Icons/new-file.png" Width="20" Height="20" Margin="0,0,5,0"/>
                            <TextBlock>New</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button ToolTip="Make a new file." Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Icons/open-file.png" Width="20" Height="20" Margin="0,0,5,0"/>
                            <TextBlock>Open</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button ToolTip="Make a new file." Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Icons/save-file.png" Width="20" Height="20" Margin="0,0,5,0"/>
                            <TextBlock>Save</TextBlock>
                        </StackPanel>
                    </Button>
                </ToolBar>
            </ToolBarTray>

            <Grid Margin="20" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="9*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="10*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <Label x:Name="lblGridTitle" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="Hit the griddy" Margin="0,0,0,0" FontFamily="Century Gothic" FontSize="36" Height="100" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

                <Grid x:Name="gdColRow" Grid.Row="1" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Grid x:Name="gdCol" Grid.Row="0" Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="6*" />
                        </Grid.ColumnDefinitions>
                        <Label x:Name="lblCol" Grid.Column="0" Content="Columns:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Name="btn_Col_Increase" Foreground="Gray" Content="▲" Width="30" Height="30" Click="Increase_Click" />
                            <TextBox Name="tbColAmount" Width="100" Height="30" Text="1" TextAlignment="Center" />
                            <Button Name="btn_Col_Decrease" Foreground="Gray" Content="▼" Width="30" Height="30" Click="Decrease_Click"/>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="gdRow" Grid.Row="0" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="6*" />
                        </Grid.ColumnDefinitions>
                        <Label x:Name="lblRow" Grid.Column="0" Content="Rows:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Name="btn_Row_Increase" Foreground="Gray" Content="▲" Width="30" Height="30" Click="Increase_Click" />
                            <TextBox Name="tbRowAmount" Width="100" Height="30" Text="1" TextAlignment="Center" />
                            <Button Name="btn_Row_Decrease" Foreground="Gray" Content="▼" Width="30" Height="30" Click="Decrease_Click"/>
                        </StackPanel>
                    </Grid>

                </Grid>

                <DataGrid Name="dgContent" Grid.Column="2" Grid.Row="2" Margin="20,20,20,20" HeadersVisibility="All">
                    <DataGrid.RowHeaderStyle>
                        <Style TargetType="DataGridRowHeader">
                            <Setter Property="Content" Value="{Binding Title}" />
                        </Style>
                    </DataGrid.RowHeaderStyle>
                </DataGrid>
                <DataGrid Name="dgPages" Grid.Row="2" Margin="20,20,20,20"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
